/** Discord application ids are snowflakes: a decimal integer, 15-25 digits today. */
export const DISCORD_APPLICATION_ID = /^\d{15,25}$/;

/** ThinkRail's own registered Discord application, so the presence has a name to wear out of the box. */
export const THINKRAIL_DISCORD_APPLICATION_ID = "1542175111384404138";
